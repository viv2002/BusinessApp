<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.businessapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BusinessApp">
        <activity android:name=".LoginActivity" />
        <activity android:name=".BusinessActivity" />
        <activity android:name=".StockManagement" />
        <activity android:name=".AccountManagement" />
        <activity android:name=".StockSearch" />
        <activity android:name=".AddSoldRecord" />
        <activity android:name=".CustomerManagement" />
        <activity android:name=".CustomerSearch" />
        <activity android:name=".UpdateCustomerRecord" />
        <activity android:name=".AddCustomerRecord" />
        <activity android:name=".UpdateStockData" />
        <activity android:name=".CustomerList" />
        <activity android:name=".StockList" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>